#include <iostream>
#include <vector>
using namespace std;
#define ll long long int
int main() {
    int t;
    cin >> t;
    while(t--) {
        int n;
        ll d;
        cin >> n >> d;  // n = number of eggs, d = number of days
        vector<int> a(n);
        for(int i = 0; i < n; i++)  cin >> a[i];

        for(int i = n-1; i >= 0; --i) {
            
        }
    }
    return 0;
}